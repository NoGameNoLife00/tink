#ifndef TINK_COPYABLE_H
#define TINK_COPYABLE_H

namespace tink {
    class copyable
    {
    protected:
        copyable() = default;
        ~copyable() = default;
    };
}
#endif //TINK_COPYABLE_H
